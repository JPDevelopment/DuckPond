/*I tried to mimic an ENUM datatype instead of just accepting string inputs to better
  guard against invalid user input.*/
const facing = {
  N: 'N',
  E: 'E',
  S: 'S',
  W: 'W'
}

module.exports = facing
